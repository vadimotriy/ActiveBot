# 🌱 Я проектирую свое благополучие — Телеграм-бот для улучшения благополучия

> **Я проектирую свое благополучие** — ваш персонаальный помощник в заботе о ментальном и физическом здоровье.  
> Через простые и дружелюбные взаимодействия в Telegram бот помогает формировать полезные привычки, отслеживать настроение, напоминать о перерывах и вдохновлять на заботу о себе.

---

## 🧠 О проекте

**Я проектирую свое благополучие** — это Telegram-бот, разработанный с целью поддержки пользователей в улучшении их повседневного благополучия.  
Бот предлагает:

-   Ежедневные задания и советы по улучшению физического благополучия
-   Навигатор помощи, дневник настроения и инструкцию что делать при тревоге
-   Таймер, профориентационные тест на основе искусственного интеллекта и советы по улучшению социального благополучия
-   Напоминания каждый день

### 🛠 Технологический стек

-   **[aiogram](https://docs.aiogram.dev/)** — асинхронный фреймворк для создания Telegram-ботов на Python
-   **[aiohttp](https://docs.aiohttp.org/)** — для потенциальной интеграции с внешними API
-   **[python-dotenv](https://pypi.org/project/python-dotenv/)** — безопасная загрузка переменных окружения
-   **[OpenRouter](https://openrouter.ai/)** — бесплатное API для нейросетей
-   **Python 3.10+**
-   **SQLite** — лёгкая БД для хранения пользовательских данных

---

## 🚀 Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/vadimotriy/ActiveBot.git
cd ActiveBot
```

### 2. Установка зависимостей

Рекомендуется использовать виртуальное окружение:

```bash
python -m venv venv

source venv/bin/activate  # Linux/macOS
# или
venv\Scripts\activate     # Windows
```

Установите зависимости:

```bash
pip3 install -r requirements/prod.txt
```

### 3. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```bash
cp Bot/data/.env.example Bot/data/.env
```

Отредактируйте `.env` и укажитe:

-   API_KEY — Telegram Bot Token полученный от @BotFather
-   API_OPEN_ROUTER — API ключ полученный на [OpenRouter](https://openrouter.ai/)
-   ADMIN — ID пользователя Telegram, который будет отправлять ежедневные напоминания

> 💡 Убедитесь, что файл `.env` не попадает в Git (`gitignore` уже настроен).

---

## ▶️ Запуск

### Без Docker

```bash
python3 -m Bot.main
```

### С Docker

Проект содержит готовый `Dockerfile` и `docker-compose.yml`.

#### Запуск одной командой:

```bash
docker-compose up --build
```

> 🐳 Docker автоматически:
>
> -   соберёт образ с зависимостями
> -   подключит `.env`
> -   запустит бота в изолированной среде

---

## 🏗 Архитектура

### Структура проекта

```
.
├── Bot
│   ├── data
│   │   ├── .env.example                            # Образец .env
│   │   ├── advices.json                            # JSON файл с советами по физическому благополучию
│   │   ├── advices_social.json                     # JSON файл с советами по социальному благополучию
│   │   ├── answers.json                            # JSON файл с ответами бота
│   │   └── tasks.json                              # JSON файл с ежедневными заданиями
│   ├── database
│   │   ├── constants.py                            # Файл с константами
│   │   ├── database.py                             # Файл с классом, отвечающим за доступ к БД
│   ├── functions
│   │   ├── ai.py                                   # Файл с функциями отвечающими за доступ к OpenRouter API
│   │   ├── functions.py                            # Файл с удобными функциями
│   │   ├── logger.py                               # Файл с настройкой логгера
│   ├── handlers
│   │   ├── admin.py                                # Файл с хендлерами для админа (ежедневное напоминание)
│   │   ├── callbacks.py                            # Файл с хендлерами обычными
│   │   ├── handlers.py                             # Файл с хендлерами в виде колбэков
│   ├── main.py                                     # Файл запуска бота
├── .dockerignore                                   # Докеригнор
├── .gitignore                                      # Гитигнор
├── docker-compose.yml                              # Докер
├── Dockerfile                                      # Докер
├── pyproject.toml                                  # Файл с настройкой ruff
├── README.md                                       # Документация
└── requirements
    ├── dev.txt                                     # Зависимости, необходимые в разработке
    └── prod.txt                                    # Зависимости, необходимые в готовом решении
```

### Логика работы

-   При старте бот подключается к Telegram API через `aiogram`.
-   Пользовательские данные (ID, настроение, ежедневные задания) хранятся локально в **SQLite** (в папке `Bot/data/`).
-   Каждое взаимодействие — это асинхронный обработчик, зарегистрированный в роутерах `aiogram`.
-   Все ключи доступа загружаются через `python-dotenv` и никогда не хранятся в коде.

---

## 🤝 Вклад в проект

PR и идеи приветствуются! Откройте issue или отправьте pull request.

---

> 💬 _"Забота о себе начинается с малого — и **Я проектирую свое благополучие** поможет сделать этот первый шаг."_

## Рабочий прототип - @WeIlBeingBot
